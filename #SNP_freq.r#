####Nach dem im script Marker_Dens.r die density der SNP-vereteilung brechnet wird brauch ich nun selbstreden auch die häufigkeit der ind. Marker... das wird eni Spaß.

##Als erste müssen näturlich erstmal wieder alle 22 tabellen eingelesen werden

require(c(dplyr,plyr))

for (u in 1:22){
    filename <- paste('X_1135_',u,'.rda.gz')
    load(filename)
    
